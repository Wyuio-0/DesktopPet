# Local voiceclone junction (do not commit the 14 GB project)
voiceclone/

# Build output
build/
dist/

# Installer staging & output
installer_staging/
installer.7z
DesktopPet_Setup.exe
DesktopPet_v1.0.0.7z.*
build_installer.bat

# Python
__pycache__/
*.pyc
*.pyo
*.spec.bak
.pytest_cache/
.verify_tmp/

# Local test/diagnostic scratch (pytest basetemp, chars_root=True bug artifacts)
.diagtmp/
True/

# AI config (contains API key — keep local only)
**/ai_config.json

# Personal course schedule / timetable data (contains PII — never commit)
kebiao.json
schedule.json
schedule_raw.json
*_schedule*.json

# Local scratch / audit scripts
scratch_*.py
scratch_*.json

# Logs & env
*.log
.env
.env.*

# Local AI/IDE tool state (may contain session data — never commit)
**/.agents/
**/.claude/

# Android Build & Cache
android/.gradle/
android/build/
android/**/build/
android/local.properties
android/.idea/
android/.cxx/
android/dist/
android/*.iml
android/**/*.iml

