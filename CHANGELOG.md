# Changelog

本项目更新日志。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 风格。

> CI 发布时会自动提取与 tag 匹配的段落（`## [vX.Y.Z]`）作为 GitHub Release 正文；
> 若该版本段落不存在，则自动从提交历史生成。新增功能记得在 `## [Unreleased]` 下补记。

## [Unreleased]

## [v1.7.0] - 2026-09-08

### 新增

- **Android 手机端实装「课表查询」与「上课提醒」功能**：
  - **课表查询视图**：底部导航栏新增「课表」Tab，支持以原生 Compose 渲染等宽色块的周课表视图，支持左右切换周次与查看课程详情。
  - **强智教务一键导入**：与 PC 端体验对齐，支持在手机端直接粘贴教务系统抓包 JSON 快速导入与持久化课表。
  - **双重智能上课提醒**：桌宠在屏幕存活期间，若距离下节课不足 10 分钟，阿米娅会自动弹出贴心的对话气泡与语音提示，并同步推送系统状态栏高优通知。

## [v1.6.7] - 2026-09-08

### 修复

- **修复 Android 手机端应用内更新报「与已安装应用签名不同」无法安装的问题**：
  - **固化全流程统一签名证书**：将项目主签名密钥库（Keystore）内置收敛于工程目录（`android/app/keystore/release.keystore`），配置 Gradle 在 debug 与 release 阶段均使用确定性的证书签名
  - **消除 CI 动态构建签名分歧**：彻底杜绝 GitHub Actions 每次在全新虚拟环境临时生成随机 debug 证书导致的签名不一致，保证本地编译与 CI 发布的 APK 签名指纹 100% 相同（SHA-256: `5c17d24a...`），实现在线平滑覆盖安装升级

## [v1.6.6] - 2026-09-08

### 优化

- **Android 手机端应用图标重绘与全新升级（静谧深空蓝黑大头款）**：
  - **定制阿米娅精致大头半身像**：重绘采用高辨识度的阿米娅萌宠大头肖像，神情温和生动，蓝色双眸与标志性兔耳、巫师帽细节精致突出
  - **沉稳相称的深板岩蓝背景**：背景告别花哨光斑，采用与人物服饰色彩高度呼应的低调深板岩蓝至深空黑柔和微光渐变（`#2C394A`），压住沉稳质感
  - **规范适配 Android 8.0+ 自适应图标（Adaptive Icons）**：
    - 前台图层（`ic_launcher_foreground`）规范适配 108dp 画布，核心人物完整居中收纳于 72dp 安全区（Safe Zone）内，兔耳与帽檐绝不被异形遮罩裁切
    - 背景层设为深板岩蓝（`#2C394A`），无论系统采用圆形、圆角矩形（Squircle）、水滴形还是支持视差拖拽，均浑然一体，彻底根除桌面强制白盘/白框问题
  - **全套分辨率与多尺寸 ICO 覆盖**：覆盖 `mipmap-mdpi` 至 `xxxhdpi` 全套密度、512x512 商店大图（`ic_launcher-web.png`）及包含 16~256 分辨率的平滑抗锯齿多层 `app.ico`

## [v1.6.5] - 2026-09-08

### 修复

- **彻底解决 Android 手机端桌面图标显示为白底方盘的问题（全适配 Adaptive Icons）**：
  - **恢复与规范 Adaptive Icon 配置**：补全 `mipmap-anydpi-v26/ic_launcher.xml` 与 `ic_launcher_round.xml`，防止 Android 8.0+（API 26~35）将应用识别为旧版 Legacy 图标而强制合成白底圆盘/方盘遮罩
  - **分密度自适应前台立绘图层（ic_launcher_foreground）**：规范生成 mdpi（108px）至 xxxhdpi（432px）全套前台高清图层，精准内缩至 72dp 核心安全区，避免异形桌面遮罩裁切角色
  - **全透明背景层生效**：通过 `#00000000` 背景与独立前台立绘结合，在手机主屏幕上呈现真正的透明纯立绘效果，消除任何突兀的白底矩形

## [v1.6.4] - 2026-09-08

### 新增

- **Android 手机端应用内无缝更新与下载进度体验**：
  - **完全告别浏览器跳转**：彻底移除下载更新跳转外部浏览器页面的旧模式，全流程在应用内完成
  - **多重重定向流式下载引擎**：UpdateManager 内置 HTTP 302/307 自动重定向追踪，无缝对接 GitHub Releases 托管于 AWS S3 的 APK 资源，分块流式写入应用专属空间，免外部读写权限
  - **实时动态进度条与即时网速**：更新弹窗与设置卡片实时展示下载进度条（LinearProgressIndicator）、已下载量与总大小（MB）、进度百分比与即时动态下载网速（MB/s）
  - **后台下载与取消**：支持随时取消下载，或切换为后台静默下载，主界面卡片实时同步进度
  - **下载完成自动无缝调起安装**：内置 FileProvider 与 REQUEST_INSTALL_PACKAGES 权限支持，下载完成（100%）后直接自动弹出系统 Package Installer 覆盖升级确认界面，并提供一键「立即安装」常驻按钮

## [v1.6.3] - 2026-09-07

### 优化

- **桌宠应用图标背景全面透明化**：
  - 移除原深蓝色圆角矩形卡片背景与几何装饰边框，将应用图标全面替换为纯透明背景
  - 多端多分辨率应用图标同步覆盖：全尺寸 `android/app.ico`（16x16 ~ 256x256）、512x512 市场大图 `ic_launcher-web.png` 以及全套 Android 原生启动图标（`mipmap-mdpi` 至 `xxxhdpi` 的 `ic_launcher.png` 与 `ic_launcher_round.png`）
  - 保留阿米娅高清立绘造型与平滑抗锯齿边缘，悬浮于手机桌面及各色壁纸上自然美观，无任何杂色方框

## [v1.6.2] - 2026-09-07

### 新增

- **Android 手机端四大核心实用功能移植**：
  - **角色原声台词音效（VoicePlayer）**：
    - 完整移植阿米娅、予愿安洁莉娜、圣聆初雪三位干员全量语音台词（`click_*.mp3`、`greet_*.mp3`）
    - 点击桌面悬浮桌宠交互时即时播放对应语音动作音效，支持音量调节与全局静音记忆
  - **罗德岛灵感便签本（Rhodes Sticky Notes）**：
    - 手机端专属黑金极简便签卡片，支持多便签快速新建、切换与删除
    - 首行智能提取便签标题，实时字数统计
    - 400ms 防抖自动原子存盘持久化（SharedPreferences），随时随地记录重要灵感与待办事项
  - **AI 对话大脑（AmiyaBrain & ChatView）**：
    - 移动端浮动聊天面板，沉浸式对话气泡展示
    - 支持 OpenAI / DeepSeek 等标准化大模型 API，深度注入阿米娅专属性格设定
    - 智能离线降级兜底机制，弱网或无网络环境下依然提供丰富的角色暖心互动台词
  - **专注番茄钟（PomodoroTimer & PomodoroView）**：
    - 25 分钟标准专注 / 5 分钟休憩倒计时陪伴
    - 悬浮环形进度与实时时间显示，专注与休憩阶段转换时触发干员原声语音提醒与桌面气泡关怀
- **Android 端版本检查与在线更新（UpdateManager）**：
  - 接入 GitHub Releases 自动版本探针，一键检查最新版本并提供更新日志弹窗与直链下载
- **精美应用专属图标**：
  - 设计并部署全套 Android 自适应与圆形图标（mdpi 至 xxxhdpi 及 512x512 Store 图标）与全尺寸 `android/app.ico`
- **角色官方命名校准**：
  - 校准为「阿米娅」、「予愿安洁莉娜」与「圣聆初雪」
- **CI/CD 全自动化多端打包发布**：
  - GitHub Actions 流水线在推送版本标签时，自动同时构建 Windows 安装包、绿色免安装 Zip 以及 Android APK，并在单次 Release 中统一挂载发布

### 优化

- **动作播放速度校准与自由调速模块**：
  - **角色动作真实速度全量校准**：逐帧测算安洁莉娜、阿米娅、圣聆初雪所有动作真实 PTS 时长与帧间隔，修复 WebM 容器虚标 60fps / 1000fps 导致的动作超速快进问题，同时微调提速 ~18%，观感更加轻盈丝滑自然
  - **动作速度即时调节模块**：
    - **右键菜单一键换档**：右键「动作互动 ▶」新增「动作速度 ▶」二级菜单，预设 0.8x（从容）、1.0x（原速）、1.15x（轻快推荐）、1.3x（敏捷）、1.5x（极速）及「自定义速度…」，点击即时变频生效
    - **设置面板实时无级滑块**：「设置… -> 通用」新增 50% ~ 200%（0.50x ~ 2.00x）动作速度滑杆，拖拽即时预览桌宠变频效果，支持一键重置，跨会话记忆

## [v1.5.0] - 2026-09-06

### 优化

- **右键上下文菜单精简与层级重构**：
  - **精简主菜单层级（28行 → 11项）**：彻底解决菜单铺满大半个屏幕过长过杂的问题，大幅收敛根菜单高度，视觉更精致紧凑
  - **四大逻辑功能区**：
    - **日常互动**：保留高频的「聊天…（Alt+A）」与「灵感便签…（Alt+N）」，将打招呼/施放技能/坐下休息整合入「动作互动 ▶」子菜单，并支持依据当前坐立姿态自适应切换“坐下休息 / 站起身来”
    - **学业与效率工具**：课程表、待办与考试、专注小工具并列清晰；将割裂的「翻译剪贴板」与截图 OCR 统一归入「翻译与识图 ▶」
    - **智能与角色**：将博士档案本、忘记当前对话、模型配置、应用白名单与 AI 在线状态统一归入「AI 与档案 ▶」；整合静音/音量滑块/TTS/语音克隆为「声音与克隆 ▶」
    - **系统管理**：保留底栏「设置…」与「退出」，移除过度滥用的分割线，菜单整体清爽协调

### 新增

- **极简悬浮灵感便签（Rhodes Sticky Note & Scratchpad）**：
  - **全局快捷键与右键呼出**：按下全局快捷键 `Alt+N`（可在设置中自定义）或右键菜单选择「灵感便签…」，随时呼出/隐藏半透明黑金悬浮便签窗口
  - **优雅无边框与自由调节**：采用罗德岛黑金 UI 设计语言，支持鼠标拖拽顶部标题栏任意移动位置，支持右下角自由缩放尺寸，窗口位置与大小关闭后自动保存并记忆
  - **实时防抖自动存盘**：输入即以 400ms 防抖自动原子写入 `%APPDATA%\AmiyaPet\notes.json`（支持 `PET_NOTES_FILE` 隔离），带实时字数统计与保存时间戳提示，永不丢失字句
  - **多便签管理**：支持一键新建便签、下拉快速切换便签、删除便签、以及窗口置顶（📌）锁定切换
  - **便签内 AI 智能联动**：便签右上角提供 🤖 AI 操作菜单，支持一键将便签中的待办事项提取并加入日程待办本，或让阿米娅帮博士润色/总结笔记内容
  - **AI 对话工具调用**：在 `actions` 中扩展 `create_sticky_note` 与 `read_sticky_notes` 工具，阿米娅在对话中即可帮博士记录灵感或查阅便签内容

- **听歌感知与音乐律动（Desktop Music Visualizer & Beats Reaction）**：
  - **WASAPI 极轻量音频峰值采样**：基于 Windows Core Audio API（纯 ctypes 零依赖，微秒级执行，CPU 占用趋近 0%），实时捕获系统音频输出的即时能量峰值（0.0 ~ 1.0），全系统音乐播放器与网页视频通用生效
  - **金色音乐音符悬浮粒子视效（MusicNotesOverlay）**：当系统播放音乐时，桌宠身旁轻盈升腾淡金色音符（♪ ♫ ♩ ♬），随音乐节奏上下跃动与轻摆；采用纯透明与点击穿透层，绝对不阻碍鼠标点击
  - **主流播放器曲目识别**：自动探测网易云音乐、QQ 音乐、酷狗、Spotify、B 站等播放器标题，解析正在播放的歌名与歌手
  - **AI 音乐感知工具**：在 `actions` 中扩展 `get_current_music()` 工具，支持在与阿米娅对话中随时询问“我现在在听什么歌”，阿米娅可感知并分享对该曲目的互动感受
  - **偏好设置独立开关**：在设置面板通用页提供「开启听歌感知与音乐音符律动（随系统音乐跳动）」开关，配置自动持久化并实时响应

- **本地天气感知与昼夜联动（Weather & Ambient Sensing）**：
  - **轻量免密实时天气感知**：集成轻量开源接口（`wttr.in`，支持根据 IP 自动定位或在设置中手动指定城市），通过独立后台 `QThread` 异步拉取天气状况、气温、湿度与风速，零阻塞主界面动画
  - **恶劣/特殊天气主动关怀**：检测到下雨、降雪或气温骤降（≤ 5°C）时，桌宠主动播放动作、弹出身旁气泡并进行语音播报（如带伞/添衣提醒）；内置 4 小时静默冷却保护，避免同类天气重复弹窗骚扰
  - **深夜环境护眼调光**：夜间（23:00~06:00）自动对桌宠画面进行平滑亮度衰减（`_dim_factor = 0.75`），在保持透明度通道与点击穿透掩码完全无损的前提下柔和降光，夜晚使用更护眼舒适
  - **AI 对话查天气支持**：在 `actions` 中扩展 `get_weather(city)` 工具，支持在与阿米娅对话中随时随地询问本地或异地城市（如北京/上海/广州等）的实时天气与出行建议
  - **偏好设置集成**：在设置面板通用页提供「开启天气感知与雨雪关怀提醒」、「深夜自动护眼调光」独立开关及城市自定义输入框，配置自动持久化并实时响应

- **长程记忆与“博士档案本”（Doctor Profile & Long-term Memory）**：
  - **跨会话持久化记忆**：突破 8 轮历史上下文限制，将博士的个人称呼、身份阶段、喜好偏好、作息习惯、当前目标与重要随记持久化保存于 `%APPDATA%\AmiyaPet\doctor_profile.json`，即使重装或升级软件也不丢失
  - **大模型 System 上下文智能注入**：每次与阿米娅对话时，自动将博士档案与近期记忆条目动态注入大模型 System Prompt，让阿米娅在交谈中自然体现对博士的记忆与了解，越聊越懂你
  - **AI 记忆工具扩展**：在 `actions` 中新增 `update_doctor_profile`（更新档案字段）、`remember_doctor_fact`（记入随记事实）、`query_doctor_profile`（查询档案）三项工具，支持大模型在交谈中主动记忆博士告知的信息
  - **可视化「博士档案本」管理面板**：右键主菜单新增「博士档案本…」（设置面板亦可直达），提供罗德岛终端风格卡片界面，支持直观查看阿米娅记住了什么，并支持手动添加、编辑或删除特定记忆条目，记忆完全透明可控

- **久坐关怀与健康提醒（Sedentary Care）**：
  - **键鼠无感活跃监测**：基于 Windows 原生 `GetLastInputInfo` 零依赖检测键盘与鼠标活跃/闲置状态，实时统计连续工作时长
  - **久坐主动关怀**：连续工作达到设定阈值（默认 60 分钟，可选 45 / 60 / 90 / 120 分钟）时，桌宠主动播放动作、弹出温馨关怀气泡并进行语音播报（支持 TTS）
  - **离岗休息自动重置**：检测到离开电脑（闲置 10 分钟以上）自动判定为休息，返回电脑重新操作时连续工作时长自动归零重计
  - **防打扰缓冲**：发出提醒后自动进入 30 分钟防打扰缓冲期；设置对话框（SettingsDialog）通用页提供独立开关与时间间隔调节

- **屏幕边缘漫游与任务栏吸附（Wandering & Snapping）**：
  - **空闲漫步（Wandering）**：空闲（idle）状态下，周期性以平滑步进向左/向右散步一段距离（播放 `move` 动作，向左移动时画面水平镜像翻转，且点击穿透掩码完全同步）；碰撞屏幕边界自动折返；遇点击、拖拽、聊天或专注模式时即时无感打断
  - **任务栏吸附与坐下（Taskbar Snapping & Docking）**：拖拽释放时检测屏幕可用工作区边界与任务栏上沿，若距离在阈值内自动贴合脚底并播放坐下（`sit`）动作，呈现坐在任务栏上的效果
  - **偏好设置开关**：在设置对话框（SettingsDialog）通用页提供「允许空闲时在屏幕边缘漫游」与「靠近任务栏时自动吸附并坐下」独立开关，配置自动持久化并实时响应

- **AI 电脑操控权限扩展**（均为白名单内安全可逆操作）：
  - **程序扩展 + 自定义白名单**：内置浏览器/微信/QQ/网易云/Steam/终端等常用程序
    （走 Windows App Paths 解析），并支持 `%APPDATA%\AmiyaPet\apps.json`
    配置任意程序
  - **电脑状态查询**（只读）：前台窗口、电量、CPU/内存/磁盘占用、开机时长
  - **键盘打字**：在当前焦点窗口模拟输入（SendInput Unicode，中文可用），
    误输可 Ctrl+Z 撤销
  - **剪贴板读写**：读取（限 2000 字）/写入（限 10000 字）
  - **窗口管理**：列出窗口、Alt+Tab 切换、最小化/最大化/关闭/聚焦/置顶指定窗口
  - **应用白名单管理**：右键菜单 → 应用白名单…，图形化添加/删除阿米娅可打开的
    自定义程序（写入 `%APPDATA%\AmiyaPet\apps.json`）
- **信息面板课程表 Markdown 风格化**：今天/本周/下一节改为富文本列表——
  金色星期标题、项目符号、加粗节次时刻、灰色地点周次、红色「本周不上」标记
- **系统托盘常驻**：关闭窗口不再退出，改为隐藏到托盘（Windows 关机/真退出除外）；
  托盘菜单可快速显示/隐藏、聊天、开关语音合成、静音、启动/停止语音克隆服务、
  退出；托盘提示实时显示语音克隆状态——语音克隆服务生命周期随进程走，
  不再因误关窗口而残留或失控
- **敏感操作确认**：AI 执行截图 / 剪贴板读取 / 键盘打字前弹确认框（说明隐私
  风险），可勾选「不再询问，总是允许」持久化；拒绝则拦截——内容不再无条件
  离开本机或被注入前台窗口
- **统一设置对话框**（右键 → 设置…）：语音（朗读开关/静音/音量）、热键
  （聊天/翻译/OCR 可视化改键，按角色保存即时生效）、通用（默认角色/考试徽章）
- **语音克隆体验**：模型加载完成时气泡提示「语音克隆模型已就绪」；设置里可勾选
  「按钮启动的语音克隆 10 分钟未使用自动停止」（默认关闭，保持手动服务常驻）
- **自动更新**：启动数秒后静默检查 GitHub Releases，发现新版本托盘提示、
  点击直达下载页；设置里可关闭或点「检查更新…」立即查（当前版本 v1.4.0，
  发版时与 tag 同步）
- **RAG 升级**：讲义检索由单字词频升级为 **n-gram TF-IDF + 余弦排序**（零依赖，
  更准）；可选**本地语义检索**——`tools/setup_knowledge_embeddings.py` 一键安装
  sentence-transformers 后源码运行自动启用多语言 embedding（打包版不内置 torch，
  自动回退词频）；设置 → 通用可开关
- **位置记忆**：拖拽停靠位置自动保存，重启恢复；多显示器支持——保存位置在
  任意屏幕内就还原，外接屏拔掉/越界时自动回退主屏底部居中
- **单实例守护**：重复启动时激活已有实例（回到前台）并自动退出新实例——防止
  两个桌宠抢全局热键/双份动画（命名互斥体 + 注册窗口消息广播）
- **安装包体积优化（315MB → 193MB）**：角色素材此前在 exe 旁与 `_internal\`
  各打包一份（且本地多次拷贝还会嵌套成三份）。现在素材只放 exe 旁唯一一份——
  spec 不再把角色打进 `_internal\`，CI 构建后显式同步到 exe 旁（本地 build.ps1
  同路径），安装包/zip 体积直接减少约 40%

### 变更

- **PetWindow 关注点分离 (SoC) 重构**：将原 2174 行的 `PetWindow` 上帝类按单一职责原则拆解为 4 个独立协调器：
  - `PetTrayCoordinator`（`pet/tray.py`）：系统托盘管理与关闭拦截
  - `PetContextMenuBuilder`（`pet/menu.py`）：右键菜单组装与角色缓存懒加载
  - `PetFocusToolsManager`（`pet/focus.py`）：番茄钟、倒计时、课表提醒、待办考试徽章与定时问候
  - `PetInputController`（`pet/input_controller.py`）：输入框、语音气泡与打字机流式动效
  主窗口精简至 1166 行，保留全部门面属性与方法，100% 保持向后兼容。

### 修复

- AI 查课表「本周」与视觉版一致：只列当周真正上课的课，学期未开始时按第 1 周
  并注明（此前 week_no=0 会把整学期课程当成本周回答）
- 敏感操作确认 30 秒无响应兜底由「放行」改为「拒绝」——宁可漏执行，不在用户
  无法确认时静默执行截图/剪贴板读取/键盘打字
- 清理死代码：timetable 未用常量/属性、info_panel 周视图旧 HTML 分支及未用导入
- **动画 CPU 占用优化（10% → 常驻约 1-2%）**：根因是帧缓存预算按"压缩后大小"
  记账，但首遍缓存却按原始 BGRA 大小记账——约 26 帧就撑爆预算导致缓存中止，
  每轮循环都在 60fps 全量重新抠图（floodFill + 连通域分析）。修复：首遍存原始帧
  （保持原生帧率原速播放），预算按估算压缩大小记账使整段循环可完整入缓存，
  重放期后台逐帧压成 PNG 腾内存；隐藏到托盘时动画零开销；内存跳帧时不再重复重绘
- 设置对话框保存后桌宠不再退出：热键重注册推迟到对话框关闭后的主事件循环
  （在模态嵌套事件循环里操作 RegisterHotKey / 原生事件过滤器会导致原生层崩溃，
  进程无痕退出）；保存异常也会写入 pet.log 并弹窗提示
- 设置对话框颜色风格统一：补齐 QTabWidget 页签、QSlider 滑杆、
  QKeySequenceEdit 组合键输入框的 Terminal 主题样式
- 切换人物菜单人物重复：角色多目录（exe旁/用户数据/内置）扫描按键去重，
  同名角色只列一次

## [v1.3.2] - 2026

### 修复

- add_character 测试在 `tmp_path/src` 下未先建目录，导致 CI 报 FileNotFoundError

## [v1.3.1] - 2026

### 新增

- **一键添加新角色**：右键 → 切换人物 → 添加新角色…，选择素材文件夹（按动作子目录
  或文件名关键词自动分类），自动复制视频/语音并生成 `config.json`

## [v1.3.0] - 2026

### 新增

- **一键语音克隆微调**：`tools/train_clone.bat` 双击即用——准备音频+文本标注即可
  自动预处理并训练 GPT-SoVITS S2（含安洁莉娜数据管线脚本）
- **RTX 5060（Blackwell）训练兼容方案**：单进程训练、假 DDP（Windows torch 无
  NCCL）、禁用 GradScaler / TF32 / cuDNN / fp16 / weight_norm，修复 backward 段错误

### 工程

- 安装包 CI 自动化：choco 安装 Inno Setup 后直接调用 ISCC（不再依赖失效的 iscc-action）

## [v1.2.1] - 2026

### 新增

- **考试倒计时常驻徽章**：桌宠旁显示距最近考试还有几天，每小时刷新，可开关
- **AI 今日安排汇总（`today_summary`）**：一句"今天有什么安排"即汇总今日课程、待办与最近考试
- **AI 打通课表/待办**：`query_schedule` / `query_tasks` / `add_task` 工具，
  阿米娅能直接回答"下节课是什么""今天有什么作业"，并能自然语言添加作业/考试截止
- **讲义知识库（RAG）**：讲义 `.txt` / `.md` 放入 `%APPDATA%\AmiyaPet\knowledge\`
  即可基于课件答疑（词频检索，零依赖）

### 修复

- 语音克隆 `clone_dir` 改为绝对路径，修复 exe 运行时找不到部署目录

### 工程

- 发行说明自动生成：优先提取 CHANGELOG 对应版本段落，缺失时从提交历史生成
- **安装包分发**：Inno Setup 生成 `DesktopPet-Setup.exe`（用户级安装、自动建桌面快捷方式）；
  打包改为 onedir（修复 onefile 下 Qt5Core.dll 加载崩溃 0xc0000409）

## [v1.1.1] - 2026

### 新增

- **信息面板**：课程表 / 待办与考试 / OCR 结果集中展示，可拖动、缩放、滚动，
  待办行内完成/删除
- **OCR 截图翻译/总结**：`Alt+S` 框选屏幕区域 → 识别文字 → 翻译或 AI 总结
  （Windows 离线 OCR 或 AI 视觉）
- **待办与考试**：自然语言添加 DDL（"周五交高数作业"）与考试倒计时，到期前语音提醒
- **课程表**：导入强智教务等高校课表，上课前提醒、查今天/下一节/本周

### 修复

- 右键菜单卡顿：语音克隆状态改为后台探测 + 缓存，消除最长 600ms 的 UI 冻结
- 抠图算法重写：清除角色动画中手臂/裙摆/发丝间的背景缝，黑块不再闪烁
- 构建脚本编码问题；打包隐私加固（`ai_config.json` 不进 exe/dist）

### 工程

- pytest 测试套件 + GitHub Actions CI（安全扫描 / 测试 / 构建 exe / 自动发布）
- pre-commit 密钥扫描钩子；MIT License；分支保护

## [v1.0.3] - 2026

### 修复

- 语音克隆服务改为手动拉起，降低常驻内存占用
- 主题适配系统浅色/深色模式
